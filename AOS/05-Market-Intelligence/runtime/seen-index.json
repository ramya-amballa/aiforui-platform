{
  "schema": {
    "key": "string — sha256(link, or source|title when there's no link)[:16]",
    "firstSeen": "string — ISO 8601 date",
    "source": "string",
    "title": "string"
  },
  "seen": {}
}
