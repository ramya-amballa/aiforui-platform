{
  "_comment": "Per-connector configuration. 'ready' connectors call a real public API today; 'connectorReady' ones have the full pipeline wired but need credentials or a target list before they return anything. Fill in the empty lists/keys to activate a connector, or set the equivalent environment variable instead (see config/credentials.template.env) — either way, no code changes needed. See ../../CONNECTOR-CONFIGURATION-GUIDE.md for full activation steps and ../integration-status-dashboard.md for current status.",
  "greenhouse": {
    "status": "ready",
    "boardTokens": [],
    "_note": "Greenhouse's public Job Board API (boards-api.greenhouse.io) has no cross-company search; add each company's board token (the slug in their /jobs page URL) to search it. Or set GREENHOUSE_BOARD_TOKENS as a comma-separated list."
  },
  "lever": {
    "status": "ready",
    "companies": [],
    "_note": "Lever's public Postings API (api.lever.co) is per-company, same as Greenhouse; add each company's Lever slug. Or set LEVER_COMPANIES as a comma-separated list."
  },
  "ashby": {
    "status": "ready",
    "jobBoardNames": [],
    "_note": "Ashby's public Job Board API (api.ashbyhq.com/posting-api) is per-organisation; add each org's job board name. Or set ASHBY_JOB_BOARD_NAMES as a comma-separated list."
  },
  "remoteok": {
    "status": "ready",
    "_note": "RemoteOK's public API (remoteok.com/api) is a single global feed of remote listings; no per-company config needed."
  },
  "upwork": {
    "status": "connectorReady",
    "apiKey": null,
    "apiSecret": null,
    "refreshToken": null,
    "graphqlQuery": null,
    "_note": "Upwork's job-search API requires OAuth2 (an approved developer app plus a refresh token obtained via a real Upwork user authorizing it) and is queried via GraphQL. The connector's OAuth2 token-refresh and GraphQL request code is real and wired; it returns nothing until apiKey/apiSecret/refreshToken are supplied. graphqlQuery lets you override the connector's default query if Upwork's schema has changed — see collectors/upwork.py's docstring and CONNECTOR-CONFIGURATION-GUIDE.md. Prefer setting these via environment variables (UPWORK_API_KEY, UPWORK_API_SECRET, UPWORK_REFRESH_TOKEN) rather than committing real values here — see config/credentials.template.env."
  },
  "linkedinJobs": {
    "status": "connectorReady",
    "apiKey": null,
    "_note": "LinkedIn has no public, unauthenticated job-search API, and its Terms of Service prohibit scraping. This connector only activates against LinkedIn's official Jobs API/Talent Solutions, if and when that access is provisioned. Or set LINKEDIN_JOBS_API_KEY."
  },
  "googleJobs": {
    "status": "connectorReady",
    "apiKey": null,
    "_note": "Google for Jobs has no direct public API; this connector is wired for a third-party search API (e.g. a jobs-focused SERP provider) once a provider and key are chosen."
  },
  "wellfound": {
    "status": "connectorReady",
    "apiKey": null,
    "_note": "Wellfound (AngelList Talent) has no public API; this connector activates once an access method is identified. Or set WELLFOUND_API_KEY."
  },
  "flexjobs": {
    "status": "connectorReady",
    "apiKey": null,
    "_note": "Connector-ready, as specified: FlexJobs requires a paid account and has no public API."
  },
  "uaeRecruiters": {
    "status": "connectorReady",
    "careerPageUrls": [],
    "_note": "Career pages vary per recruiter/agency with no shared API; add each real career-page URL to monitor once identified."
  },
  "consultingFirms": {
    "status": "connectorReady",
    "careerPageUrls": [],
    "_note": "Same as uaeRecruiters: add each firm's real career-page URL once identified."
  }
}
